---
title: Example MDX with frontmatter
date: 2026-02-20
author: Food Diaries
---

# Example MDX with frontmatter

This content is written in **MDX**. The block above is YAML frontmatter — it is parsed by `remark-frontmatter` and `remark-mdx-frontmatter` and exposed as a named export when you import this file.

You can use _markdown_ and [links](https://nextjs.org/), and embed React components from `mdx-components.tsx`:

<Counter />
